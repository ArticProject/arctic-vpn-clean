workflows:
  android-build:
    name: Android Build
    environment:
      flutter: stable

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release --no-tree-shake-icons

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
      
