name: arctic_vpn
description: Arctic VPN - fake to real VPN app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1      # или последняя версия (на февраль 2026 — 2.5.x)
  shared_preferences: ^2.3.0    # для сохранения темы

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # Добавь позже assets: и openvpn_flutter
